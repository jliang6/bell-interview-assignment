/**
 * A function to calculate the total price by
 * 1) calculate the total cost for the items
 * 2) deduct discount based on the quantity
 * 3) add sales tax based on the province/state code
 * @param {string} name
 *   The name to say hello to.
 * @return {string}
 *   A hello.
 */
export {
  CalTotalPriceInput,
  calTotalPriceByInput,
  calTotalPrice
} from "./calTotalPrice";
